# Trading Analysis MCP Server Requirements
# Core data processing (for preprocessing only)
pandas==2.2.2
numpy>=1.21.0
tqdm==4.66.4

# Ngrok integration for public URL exposure
pyngrok==7.0.0

# MCP server uses only built-in Python modules:
# - http.server (built-in HTTP server)
# - urllib.parse (built-in URL parsing) 
# - json (built-in JSON handling)
# - os, sys (built-in system modules)